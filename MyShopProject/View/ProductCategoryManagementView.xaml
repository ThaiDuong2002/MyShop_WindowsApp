<Page x:Class="MyShopProject.View.ProductCategoryManagementView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyShopProject.View"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{StaticResource ProductCategoryManagementVM}"
      FontFamily="{StaticResource MaterialDesignFont}"
      xmlns:Converter="clr-namespace:MyShopProject.Converters"
      Title="QLLOAISP">

    <Page.Resources>
        <Converter:ImgBrandConverter x:Key="ImgBrandConverter"/>
    </Page.Resources>

    <Grid VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Padding="20,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" TextAlignment="Left" FontSize="24" FontWeight="Bold"  Margin="0 20 0 0"> DANH SÁCH NHÃN HÀNG</TextBlock>
        <Button Command="{Binding AddCommand}" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 20 20 0" Content="Thêm nhãn hàng" x:Name="addCustomerButton" Background="BlueViolet" BorderThickness="0"/>
        <TextBlock Name="amountCustomerText" Grid.Row="1" Text="{Binding AmountProduct}" Padding="20,40,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" TextAlignment="Left" FontSize="18" FontWeight="Bold"  Margin="0 20 0 0"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0 10 20 0">
            <StackPanel Orientation="Horizontal">
                <Button
                    Command="{Binding EditCommand}"
                    Content="Sửa"
                    Background="Green"
                    Margin="20 40 0 0"
                    BorderThickness="0"
                    />
                <Button
                    Command="{Binding DeleteCommand}"
                    Content="Xóa"
                    Margin="20 40 20 0"
                    Background="Red"
                    BorderThickness="0"
                    />

                <StackPanel VerticalAlignment="Bottom" >
                <TextBox Text="{Binding SearchCustomerText, UpdateSourceTrigger=PropertyChanged}" Name="searchCustomerInput" materialDesign:HintAssist.Hint="Tìm kiếm tên nhãn hàng" BorderBrush="{DynamicResource MaterialDesignDivider}" Height="25" HorizontalAlignment="Left"  VerticalAlignment="Center" Width="200" />
            </StackPanel>
            <Button Command="{Binding searchBtn}" Margin="20 40 0 0" Content="Tìm kiếm" x:Name="searchCustomertButton" Background="BlueViolet" BorderThickness="0" Focusable="False"/>
            </StackPanel>
        </StackPanel>

        <ListView MinHeight="500" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Name="listView" Grid.Row="2" ItemsSource="{Binding Brands,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedBrand}" SelectionMode="Single" Margin="20" BorderThickness="0" >
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card
                        Padding="10"
                        Margin="10"
                        Width="250"
                        Height="250"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="White"
                        BorderThickness="0"
                        materialDesign:ShadowAssist.ShadowDepth="Depth1"
                        >
                        <StackPanel Orientation="Vertical">
                            <Image Source="{Binding Id,Converter={StaticResource ImgBrandConverter}}" Width="180" Height="180" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Mã loại sản phẩm: " FontSize="16"/>
                                <TextBlock Text="{Binding Id}" FontSize="16" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Tên nhãn hàng: " FontSize="16"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="Red"  />
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                </DataTemplate>


            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Name="pagination" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Command="{Binding prevBtn}" Content="Trang trước" Background="BlueViolet" BorderThickness="0" Margin="10"/>
            <Label x:Name="contentPage" Content="{Binding PageInfo}" FontSize="16" BorderThickness="0" Margin="10"/>
            <Button Command="{Binding nextBtn}" Content="Trang sau" Background="BlueViolet" BorderThickness="0" Margin="10"/>
        </StackPanel>
    </Grid>
</Page>
