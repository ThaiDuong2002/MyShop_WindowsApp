<Window x:Class="MyShopProject.View.AddBrandView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyShopProject.UserControls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        DataContext="{StaticResource AddBrandVM}"
        Title="AddBrandView" Height="450" Width="500">
    <Grid>
        <local:BrandUserControl/>
        <Button Content="Thêm nhãn hàng" HorizontalAlignment="Center" Margin="10,30,0,10" VerticalAlignment="Bottom" Height="30" Command="{Binding AddBrandCommand}"/>
    </Grid>
</Window>
